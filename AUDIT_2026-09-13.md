# AUDIT 2026-09-13 — "মডেল গুলো ফিউচার ক্যান্ডেল দেখানোর কথা কিন্তু দেখাচ্ছে না"

ইউজারের প্রশ্ন দুটি:
1. মডেলগুলো ফিউচার ক্যান্ডেল দেখানোর কথা — কিন্তু দেখাচ্ছে না। আসলে কোথায় সমস্যা?
2. প্রেডিকশন কেন ভুল হয়?

এই অডিট প্রতিটি লাইন ধরে ধরে উত্তর দেয়। মোট ২৩টি নথিভুক্ত সমস্যা (P=Problem),
যার মধ্যে **P1 হলো ফিউচার-ক্যান্ডেল না দেখানোর একমাত্র প্রধান কারণ** — বাকিগুলো
প্রেডিকশন-ভুল/UX শ্রেণির। প্রতিটির ফিক্স এই রিলিজেই করা হয়েছে যেখানে করা
সম্ভব ও সৎভাবে যুক্তিসঙ্গত ছিল।

---

## পর্ব ১ — ফিউচার ক্যান্ডেল দেখাচ্ছে না: আসল কারণ

### P1 (CRITICAL — THE root cause): ML মডেলের T+1/T+2 প্রেডিকশনে কখনোই ক্যান্ডেল জ্যামিতি (OHLC) ছিল না, আর ফ্রন্টএন্ড কার্ড ছাড়া কিছু দিয়েছিল না

সিস্টেমে **দুটি আলাদা প্রেডিকশন ইঞ্জিন** চলে:

| ইঞ্জিন | আউটপুট | চার্টে ভবিষ্যৎ ক্যান্ডেল? |
|---|---|---|
| পুরনো ৬-মডিউল confluence ইঞ্জিন | `stream.prediction` + `prediction.candle` (`_pred_candle()`, feed.py:367) → snapshot/tick ব্রডকাস্ট | ✅ দেখায় (ghostSeries, ১টি ক্যান্ডেল) |
| **ML মডেল T+1/T+2** (core/otc_predict) | `otc_pred` WS ফ্রেম (predictor.py:188-342) — শুধু direction/probability/target_time/score | ❌ **কখনোই না** |

`otc_pred` পেলোডে `t1`/`t2` স্লটে `target_time`, `prediction` (CALL/PUT),
`probability`, `tier`, `emit` আছে — কিন্তু **`candle` (OHLC) নেই**। ফ্রন্টএন্ডের
`onOtcPred()` (common.js:2576) শুধু `renderPredictionCard()` কে ডাকে — চার্টের
`ghostSeries`-এ হাত দেয় না। REST ফলব্যাক `fetchPredictionCard()`-ও একই।

**ফলাফল:** ইউজার চার্টে "ভবিষ্যৎ ক্যান্ডেল" দেখতে যান → দেখেন পুরনো ইঞ্জিনের
ghost ক্যান্ডেল (যেটা মডেলের নয়), আর মডেলের T+1/T+2 কেবল পাশের কার্ডে টেক্সট
হিসেবে "NO TRADE (50%)"। মডেল যত ভালোই হোক, চার্টে তার ভবিষ্যৎ ক্যান্ডেল
**দেখানোর কোনো কোড পথই ছিল না**।

**ফিক্স (এই রিলিজ):**
1. `core/otc_predict/geometry.py` (নতুন) — দুই পাথ (WS + REST) একই জ্যামিতি
   ব্যবহার করে: `future_candle(base_close, atr, direction_up, probability)`
   → সম্ভাব্য OHLC (ATR-স্কেলড body/wick, conviction-স্কেলড)।
2. `predictor.py` — `on_candle_closed()` এখন `t1.candle` / `t2.candle` পাঠায়
   (closed window-এর ATR + close থেকে, horizon অনুযায়ী সময় = signal_time + h*period)।
3. `server.py` `/api/prediction/{asset}` — frozen row-এর `close_i` + candle_micro
   হিস্টোরির ATR থেকে একই জ্যামিতি REST-এও।
4. `common.js` — `onOtcPred()` এখন T+1/T+2 ক্যান্ডেল `ghostSeries`-এ আঁকে;
   মডেল সক্রিয় থাকলে পুরনো ইঞ্জিনের ghost সাপ্রেস হয় (একই সময়-স্লটে দুইটি
   ক্যান্ডেল ওভারল্যাপ এড়াতে); target_time পেরোলে ক্যান্ডেল expire হয়;
   পেয়ার বদলালে clear; REST ফলব্যাকও এঁকে দেয়।

### P2 (CRITICAL, UX): মডেল সিগন্যাল ইমিট হওয়া গাণিতিকভাবে অসম্ভব — তাই কার্ডে সবসময় "NO TRADE"

`signal_filter.py`-এর স্কোর: `50*ml_conf + 15*momentum + 10*trend + 10*level +
5*vol + 10*structure`। সর্বোচ্চ PA (সব কম্পোনেন্ট ১.০) হলেও অ-ML অংশ = ৫০।
GOOD (≥৭০) ইমিট করতে হলে `ml_conf ≥ 0.40` লাগে ⇒ **calibrated P(UP) ≥ 0.70**
(|p−0.5|×2)। Platt-ক্যালিব্রেটেড সৎ মডেল কখনো 0.70 দেয় না (ডেটাতে edge নেই) —
তাই ২,২২,১৯২ সিদ্ধান্তে ০ emit (backtest report)। এটা PART-29 সততার নকশা,
কিন্তু ইউজার-অভিজ্ঞতায় মানে "মডেল কিছুই বলে না"।

**ফিক্স (সৎ সমাধান):** মডেলের direction + probability এখন চার্টে ভবিষ্যৎ
ক্যান্ডেল হিসেবে **সবসময়** দৃশ্যমান (emit হোক বা না হোক) — emit কেবল
"ট্রেডেবল সিগন্যাল" ফ্ল্যাগ, দিকনির্দেশ প্রদর্শনের শর্ত নয়। কার্ডও
স্পষ্ট করে বলে দিক ওঠানামা করে। (গেট-নীতি অপরিবর্তিত — ভুয়া সিগন্যাল
দেওয়া হয়নি।)

---

## পর্ব ২ — প্রেডিকশন কেন ভুল হয় (গভীর বিশ্লেষণ)

### P3: OTC ১-মিনিট ক্যান্ডেল প্রায় শুদ্ধ random walk — কোনো ইন্ডিকেটর-edge নেই

নিজস্ব ব্যাকটেস্টেই প্রমাণিত (OTC_PIPELINE.md + রিপোর্ট JSON):
- ML best (RF): T+1 acc **৫০.১১%**, T+2 **৪৯.৯৮%**, logloss ≈ ln2 — কয়েন-ফ্লিপ।
- candle-color persistence pooled follow-rate **৪৯.১৩%** (CI95 ৪৮.৮৯–৪৯.৩৬)।
- shuffle-probe ৫০.১% — লিকেজ নেই; অর্থাৎ মডেল সৎ, ডেটাতে edge নেই।
- every-candle পুরনো ইঞ্জিন: **৫০.৬%** WR (backtest_every_candle_results.json)।

এটা কোডের বাগ নয় — বাজারের সত্য। তবু দুটো সৎ উন্নতি সম্ভব ছিল:
(P4, P5 দেখুন।)

### P4: ইউজার যে সিগন্যাল দেখে সেটা মডেলের নয় — পুরনো ~50.6% ইঞ্জিনের

Signal box, হিস্টোরি, win-rate ড্যাশবোর্ড — সব `stream.prediction` (confluence
ইঞ্জিন) থেকে। ML মডেল শুধু আলাদা কার্ডে বসে ছিল। ইউজার ভাবেন "মডেল ভুল
বলছে" — আসলে ভুলটা বেশিরভাগ সময় পুরনো ইঞ্জিনের fallback-এর। **ফিক্স:** এখন
চার্টে মডেল-ক্যান্ডেল আলাদাভাবে দৃশ্যমান + কার্ডে মডেল ভার্সন/প্রোভিশনাল
ব্যাজ — কোন ইঞ্জিন কী বলছে আর গোলমাল নেই।

### P5: fallback চেইন ঐতিহাসিকভাবে উল্টো দিক নিত (body_fade) — ৪৫.৫% WR

`blender.py`-এ insufficient-data fallback এখন `body_fade` (আগের বডির বিপরীত)
ব্যবহার করে; ACCURACY-FIX-2026-09-11 এ মাপা ছিল যে follow (৪৫.৫%) এর চেয়ে fade
ভালো। এটাই রাখা হয়েছে — টাচ করিনি (গেট-সংক্রান্ত নীতি অক্ষুণ্ণ)।

### P6 (minor, FIXED): `payload["quality"]` শেষ horizon-এর দ্বারা overwrite

predictor.py:278-320 লুপে `quality` ভেরিয়েবল t1→t2 দুইবার কম্পিউট হয়,
`payload["quality"] = quality` লুপের পরে — মানে সবসময় **t2-এর** quality যায়,
t1-এর হারিয়ে যায়। **ফিক্স:** `payload["t1"]["quality"]` / `payload["t2"]["quality"]`
per-slot + টপ-লেভেল ব্যাকওয়ার্ড-কম্প্যাট t1-এর কপি।

### P7 (observed, by-design): মডেল prob≈0.5-এ কার্ড "NO TRADE (50%)" — এখন চার্টে দিকনির্দেশ স্পষ্ট

আগে ইউজারের কাছে "৫০%" মানেই "কিছুই না"। এখন ক্যান্ডেল আঁকা হয়: CALL=সবুজ
ভবিষ্যৎ ক্যান্ডেল, PUT=লাল — probability-স্কেলড body।

### P8: live micro=None (<৩ tick) হলে micro ফিচার 0.0 — ট্রেইন/সার্ভে হালকা skew

Honest degradation, ডকুমেন্টেড। স্কিউ তৈরি হলে মডেল prob আরও 0.5-এর কাছে যায় —
ভুল দিক নির্দেশের কারণ নয়। অপরিবর্তিত (সৎ আচরণ)।

### P9: `_quality_gates.no_gap` পুরো ৫০-ক্যান্ডেল উইন্ডো জুড়ে strict

কোনো এক মিনিটের gap-এ emit বন্ধ (NO SIGNAL) — training-এর gap-splitting-এর
সাথে সামঞ্জস্যপূর্ণ, সঠিক। অপরিবর্তিত।

### P10: prediction card REST পাথে engine.model_version না থাকলে status 'no_model' দেখায় যদিও frozen row আছে

`fetchPredictionCard`-এ `status: (data.engine && data.engine.model_version) ? 'ok' : 'no_model'`
— রেজিস্ট্রি খালি কিন্তু frozen rows থাকলে (restart-পরবর্তী মুহূর্ত) কার্ড
no_model রেন্ডার করে t1/t2 সত্ত্বেও। **ফিক্স:** status এখন frozen row-নির্ভর
(`has_current` থাকলে 'ok')।

### P11–P23 (সংক্ষিপ্ত নথি — সব পুরনো FIX যাচাই করা হয়েছে, প্রতিটি লাইন):

- P11: `broadcast()` otc_pred কেবল interested_cids-কে যায় — সঠিক।
- P12: freeze UNIQUE key (PART 16) — replay-প্রুফ, সঠিক।
- P13: settle_target + settle_from_history (restart-proof) — সঠিক।
- P14: prediction_analytics-এর dir_/draws KeyError ফ্যামিলি — আগেই ফিক্সড (যাচাই করলাম, ঠিক আছে)।
- P15: `_pred_candle` NEUTRAL-এ None — সঠিক (ভুয়া ক্যান্ডেল নয়)।
- P16: ghostSeries sanitize (NaN/idx) — সঠিক।
- P17: `updateChart`-এ candle dedup/sort — সঠিক।
- P18: WS reconnect-এ `_predCardAsset` stale — ensurePredictionCard পুনরায় আনে, ঠিক।
- P19: models-panel লাইভ-টেবিল — ঠিক।
- P20: fast_train ছোট-ডেটা ফোল্ড (_fast_folds) — ঠিক।
- P21: Platt calibration time-ordered tail — ঠিক, লিক-সেফ।
- P22: walk_forward EMBARGO=2 — T+2 overlap কভার, ঠিক।
- P23: shuffle-probe hard gate (FAST_SHUFFLE_MAX 0.53) — ঠিক।

---

## পর্ব ৩ — এই রিলিজে যা যা বদলালো

| ফাইল | পরিবর্তন |
|---|---|
| `core/otc_predict/geometry.py` | **নতুন** — `future_candle()` + `atr_from_candles()` — conviction-স্কেলড ATR জ্যামিতি; CALL=up-body, PUT=down-body |
| `core/otc_predict/predictor.py` | t1/t2 স্লটে `candle` + per-slot `quality`; geometry যোগ |
| `server.py` | `/api/prediction/{asset}`-এর প্রতিটি current/history row-তে `candle` (close_i + candle_micro ATR থেকে) |
| `static/js/common.js` | `modelPredCandles` স্টেট; `onOtcPred`→`setModelGhostCandles()`→`drawModelGhostCandles()`; `updateChart`-এ মডেল-প্রিসিডেন্স (stale-snapshot fallback সহ); expiry-ফিল্টার (`>=` — T+1 টার্গেট তো চলমান ক্যান্ডেলের স্লটই, পুরনো ইঞ্জিনের ghost-এর মতোই); নতুন ghost আঁকার পর `scrollToPosition(3)` (pixel-টেস্টে ধরা পড়েছিল T+2 viewport-এর বাইরে চলে যায়); পেয়ার-সুইচ/রি-ইনিট clear; REST পাথেও আঁকা; কার্ডে "চার্টে দেখানো হচ্ছে" হিন্ট; `fetchPredictionCard`-এর status সংশোধন (P10); ডিবাগ/টেস্ট হুক `__modelPred` / `__feedFrame` |
| `static/css/app-v3-panels.css` | `.pred-chart-hint` স্টাইল |
| `scripts/e2e_future_candle.py` | **নতুন** E2E (৪ ফেজ, ৪৭ অ্যাসার্ট): geometry ইউনিট → লাইভ WS payload (আসল ট্রেইন্ড বান্ডেল দিয়ে `on_candle_closed`) → REST কার্ড → **আসল Chromium ব্রাউজারে চার্ট-ড্রয়িং** (Playwright; handleMsg-এর আসল পাথ দিয়ে otc_pred ইনজেক্ট) |
| `AUDIT_2026-09-13.md` | এই নথি |

## সততার ঘোষণা (PART 29 অপরিবর্তিত)

ভবিষ্যৎ ক্যান্ডেলটি **দিকনির্দেশ + সম্ভাব্যতার ভিজ্যুয়ালাইজেশন** — ভবিষ্যতের
exact OHLC জানার দাবি নয়। কেউ তা জানতে পারে না; যে সিস্টেম দাবি করে সেটাই
স্ক্যাম। এই সিস্টেম যা করে: মডেলের frozen দিকনির্দেশকে ATR-স্কেলড ক্যান্ডেলে
এঁকে দেয়, probability যত বেশি বডি তত বড় — আর প্রতিটি ফ্রিজ ক্যান্ডেল
বাস্তব ক্লোজে গ্রেড হয়। মিথ্যা edge নয়, সৎ প্রেডিকশন-এর দৃশ্যমানতা।
