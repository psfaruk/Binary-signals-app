# Railway build configuration (Nixpacks) — LIGHTWEIGHT version
# No Playwright, no Chromium, no system deps needed.
# Pure Python + WebSocket app, <50MB RAM usage.

setup:
  - python: "3.11"

phases:
  install:
    commands:
      - pip install -r requirements.txt

start:
  command: python server.py
