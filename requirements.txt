# FIX (DEEP-AUDIT-2026-07-26 / F-19-38): added upper bound `<0.115` to
# fastapi to avoid the Pydantic v2 jump (0.115+ requires Pydantic v2
# idioms). A-10 PROBLEM 31.
fastapi>=0.110.0,<0.115
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# ── pyquotex dependencies (vendored copy in ./pyquotex/) ───────────────────
# The ./pyquotex folder is a vendored copy with local fixes:
#   - Firefox TLS cipher suite (ssl_utils.py) → bypasses Cloudflare
#   - Cookie-jar handling fixes
#   - Token parsing fixes
#   - Reconnect policy improvements
# NO curl_cffi, NO Playwright needed — httpx + Firefox TLS does the job.
beautifulsoup4>=4.12.0
certifi>=2024.0.0
fake-useragent>=1.5.0
httpx>=0.27.0
typing_extensions>=4.0.0
websockets>=12.0

# FIX (DEEP-AUDIT-2026-07-26 / F-19-39): moved audit-history notes out of
# requirements.txt (A-10 PROBLEM 33). Dependency history belongs in git
# log / CHANGELOG.md, not in a dependency manifest. The previous notes
# documented the removal of `ntplib`, `pyfiglet`, `rich` (2026-07-13) and
# the addition of `typing_extensions` — see `git log -- requirements.txt`.
